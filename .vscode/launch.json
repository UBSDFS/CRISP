{
    "configurations": [
    {
        "name": "Launch Chrome",
        "request": "launch",
        "type": "chrome",
        "url": "http://localhost/project/public",
        "webRoot": "${workspaceFolder}"
    }
    ]
}